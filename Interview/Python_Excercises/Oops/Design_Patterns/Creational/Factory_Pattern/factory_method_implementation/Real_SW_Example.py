# A software example

''' The Django framework uses the Factory Method pattern for creating the fields of a form.
The forms module of Django supports the creation of different kinds of fields
(CharField, EmailField) and customizations (max_length, required'''

#
# For example, one Factory Method might be responsible for connecting you to different databases
# (MySQL, SQLite),
# another Factory Method might be responsible for creating the
#     geometrical object that you request (circle, triangle), and so on.